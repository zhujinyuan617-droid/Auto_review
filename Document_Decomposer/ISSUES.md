# ISSUES.md — 已知问题唯一台账

> 规则见根 `CLAUDE.md`。遇到问题先查这里。每条:严重度 / 描述 / 证据 / 状态。
> 这里只记**真问题**;乐观状态不进这里(也不进别处当事实)。

## 🔴 地基级(影响下游一切)

### I1. 卡片质量不均
- 描述:连接层(词表/边/索引/出稿)全建在 `literature_card.json` 上。卡片由 AI 抽取生成,会读漏/读错;全库是不同版本管线的拼接物,早期批次在后续修复前生成、之后未重跑。
- 证据/范围:一次性 133 篇审计估「约一半干净」,但该审计**自身被发现高估+误报**(把中间文件当最终错),所以**真实比例未在冻结标准下确证**。
- 状态:未解决。**下游所有结论都受此限制。**

### I2. 全库未在同一冻结标准下重跑
- 描述:library 是不同版本管线产出的拼接物,从没用「当前最终标准」整体重跑一遍。
- 状态:未做。任何「全库已合格」的说法在此之前都不成立。

## 🟠 方法限制(设计内在,非 bug)

### I3. 判关系依赖薄摘要,矛盾召回低
- 描述:②判关系只让 AI 读卡片笼统摘要(core_question+key_findings+mechanisms),埋在细节里的张力看不见。
- 证据:`edges.json` 里 contradicts 仅 7 / 536。
- 状态:已知限制。待「高分边喂更厚上下文/原子」的定向第二遍。

### I4. ③ 概念→段落 字面召回天花板
- 描述:字符串匹配词表成员,写法变体漏(正文写「Knudsen」而词表是「knudsen diffusion」→ 漏)。
- 证据:`concept_index.json` 实测 knudsen diffusion passing=0。
- 状态:待 embedding / AI 段落匹配(原计划 ⑦)。

### I5. 空白榜是「候选」,非已证实
- 描述:`gap_score` 的 central 来自卡片标签(受 I1)、passing 来自字面匹配(受 I4),两端都不全。
- 状态:只能当线索,需人工核。

### I6. 出稿保真闸偏浅
- 描述:风格 pass 的校验只比对「引用集合 + 数字集合」是否变化,**查不出「数字保留但被接到错误论断上」**。
- 证据:`scripts/draft_section.py` 实现。
- 状态:已知弱点,语义级保真未做。

### I7. 多处启发式未调优
- 描述:facet 权重(topic0.6/method0.8/object1.0)、top-K=10、gap_score 公式、特异性阈值——全是拍的,无理论保证、未调参。
- 状态:未调,可能扭曲图的疏密与排序。

## 🟡 数据卫生(待清理)

### I8. 库内混入跑错主题/非英文/非文章
- 描述:有与主题无关的论文(如 S152 冠脉、S153 丙肝),被关联网自动孤立;非英文/书索引按设计延后。
- 证据:候选边 2 个孤立点 = S152/S153。
- 状态:待从库里清理或单独标注。

### I9. 元数据识别仍可能误判
- 描述:title/DOI/year/journal 靠脚本规则从首页/文件名/页眉识别,可能误判;最终卡片里多数对,但 `metadata_candidates.json` 是故意的候选垃圾(非真缺陷,勿当 bug)。
- 状态:已知,未系统化。

## ⚪ 方法论提醒(不是某个具体 bug)

### I10. LLM 自评不可信(含本助手)
- 描述:本台账与各处「已验证」基本都是**抽样**结论;验证者(包括 AI 审查员、包括我)本身会幻觉、会误报/过严。
- 纪律:任何 LLM 自评结论,先抽样人工/脚本复核再行动。

### I11. 风格出稿缺用户语料
- 描述:终点「贴用户真实写作风格」需用户过去的论文/综述当语料;仓库内无,不可凭空捏造。
- 状态:外部依赖,待用户提供。能力(风格 pass + 保真闸)已建,只差数据。
